import { HealthTrackingModel } from './health-tracking.model';

describe('HealthTrackingModel', () => {
  it('should create an instance', () => {
    expect(new HealthTrackingModel()).toBeTruthy();
  });
});
